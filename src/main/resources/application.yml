quarkus:
  cache:
    caffeine:
      initial-capacity: 1000
      maximum-size: 10000
  rest-client:
    tg-bot-api:
      url: 'https://api.telegram.org/bot${telegram.bot.token}'
    compute-engine:
      url: '${compute-engine.url}'
    cere-wallet:
      url: '${cere-wallet.url}'
    rob-api:
      url: '${rob.api.url:https://api.rob.stage.cere.io/rms-node-backend}'
  kotlin-serialization:
    json:
      encode-defaults: false
      ignore-unknown-keys: true
  http:
    cors:
      ~: true
      origins: '*'

agent-service:
  bot-name: '${AGENT_SERVICE_BOT_NAME:ulad_bullish_bot}'
  mini-app-name: '${AGENT_SERVICE_MINI_APP_NAME:viewer}'

"%dev":
  quarkus:
    rest-client:
      logging:
        scope: 'request-response'
        body-limit: 10000
    log:
      category:
        "org.jboss.resteasy.reactive.client.logging":
          level: 'DEBUG'